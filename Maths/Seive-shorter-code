#include <bits/stdc++.h>
using namespace std;
#define M 1000000007
 
// Shoter Implementation of the optimized Sieve

// O(nloglogn) time complexity which is nearly equivalent to O(n)

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n;
    cin >> n ;
    vector<int> isPrime(n+1, true);
    for (int i = 2 ; i <= n ; i++)
    {
        if (isPrime[i])
        {
            cout << i << endl;
            for (int j = i*i ; j<= n ; j = j+i)
            {
                isPrime[j] = false;
            }
        }
    }

    
    
}
